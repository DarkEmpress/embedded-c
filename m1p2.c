//Write a Program to Multiply Two Floating-Point Numbers entered by the user.
#include <stdio.h>
int main() {
    float x, y;
    scanf("%f %f", &x, &y);
    printf("Product = %f\n", x * y);
    return 0;

}